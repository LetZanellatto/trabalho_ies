#include <stdio.h>

int main()
{
    int a, b, i;
    scanf("%d %d", &a, &b);
    
    if (a<b){
        for(i=a; i<=b; i++)
        printf("%d\n", i);
    }
    
    else if (a>b) {
        for(i=0; i<=(a-b); i++)
        printf("%d\n", a-i);
    }
    else if (a==b)
        printf("Valores Iguais");
    
    return 0;
