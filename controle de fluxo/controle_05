#include <stdio.h>

int main(){
    float nota1, nota2, media;
    while(nota1 != 50){
        scanf("%f %f", &nota1, &nota2);
            media = (nota1 + nota2)/2;
                printf("Média = %.2f", media);
    }
    return 0;
}
